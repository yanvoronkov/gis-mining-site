# Автоматический обработчик SEO фильтров

## ✅ Что было реализовано

Добавлен автоматический обработчик в `/local/php_interface/init.php`, который:

1. **Перехватывает все запросы** к URL вида `/catalog/*/filter-*/`
2. **Находит соответствующую запись** в инфоблоке "ЧПУ" (ID: 14)
3. **Извлекает SMART_FILTER_PATH** из поля "Старая ссылка"
4. **Автоматически устанавливает** `$_REQUEST['SMART_FILTER_PATH']`
5. **Фильтр применяется** автоматически через компонент `catalog.smart.filter`

## 🎯 Теперь фильтры работают автоматически!

### Как создать новый SEO фильтр:

**1. Откройте админку Битрикс:**
```
Контент → Информационные блоки → ЧПУ
```

**2. Создайте новый элемент:**
- **Название:** Например, "Крипта BTC"
- **Активность:** ✅ Да
- **Символьный код:** `/catalog/asics/filter-btc-crypto/` (будет сгенерирован автоматически)

**3. Заполните свойства:**

| Свойство | Значение | Пример |
|----------|----------|--------|
| **Старая ссылка** | Стандартный URL фильтра Bitrix | `/catalog/asics/filter/crypto-is-btc/apply/` |
| **Новая ссылка** | Красивый ЧПУ URL | `/catalog/asics/filter-btc-crypto/` |
| **Редирект** | 301 Moved permanently | |
| **Генерировать варианты** | ✅ Включить | |
| **SEO H1** | Заголовок для SEO | ASIC-майнеры для добычи BTC (Bitcoin) |
| **SEO Title** | Meta title | Купить ASIC-майнер для BTC — каталог GIS Mining 2025 |
| **SEO Description** | Meta description | Официальные поставки ASIC-майнеров для добычи Bitcoin... |

**4. Сохраните запись**

**5. Очистите кеш:**
```
Настройки → Производительность → Очистить кеш
```

**6. Готово!** ✅

Теперь URL `/catalog/asics/filter-btc-crypto/` автоматически покажет товары с CRYPTO=BTC!

## 📋 Принцип работы

### ВАЖНО: Работает для ЛЮБЫХ URL!

Обработчик **НЕ требует** конкретного паттерна в URL. Он работает для:

✅ `/catalog/asics/filter-zec-crypto/`  
✅ `/catalog/asics/zec-asiki/`  
✅ `/catalog/videocard/nvidia-rtx/`  
✅ `/catalog/gotovyy-biznes/ltc-doge/`  
✅ **Любой другой URL в /catalog/**

### Как это работает:

**Алгоритм обработки:**

1. **Проверка каталога:**  
   - Обрабатываются только URL начинающиеся с `/catalog/`
   - Исключаются запросы к файлам (css, js, images)

2. **Поиск записи:**  
   - По точному совпадению URL с полем CODE (символьный код) записи в инфоблоке "ЧПУ"

3. **Если запись найдена:**  
   - Читается "Старая ссылка" (OLD_URL)
   - Извлекается SMART_FILTER_PATH
   - Устанавливается в $_REQUEST и $_GET

4. **Если запись НЕ найдена:**  
   - Запрос обрабатывается стандартно (это может быть раздел, товар, 404)

### Пример для фильтра ZEC:

```
1. Пользователь открывает: /catalog/asics/filter-zec-crypto/
   (или /catalog/asics/zec/ - любой URL!)
   ↓
2. OnBeforeProlog перехватывает запрос
   ↓
3. Проверяет: начинается с /catalog/? ✅
   ↓
4. Ищет запись в инфоблоке "ЧПУ" с CODE = /catalog/asics/filter-zec-crypto/
   ↓
5. Находит запись ID: 99673
   ↓
6. Читает OLD_URL: /catalog/asics/filter/crypto-is-zec/apply/
   ↓
7. Извлекает SMART_FILTER_PATH: crypto-is-zec
   ↓
8. Устанавливает $_REQUEST['SMART_FILTER_PATH'] = 'crypto-is-zec'
   ↓
9. Компонент catalog работает как обычно, но с установленным фильтром
   ↓
10. Показываются только товары с CRYPTO=ZEC ✅
```

## 🔧 Как определить "Старую ссылку"

**Метод 1: Через браузер** (рекомендуется)

1. Откройте каталог: `/catalog/asics/`
2. В фильтре отметьте нужное значение (например, BTC)
3. Нажмите "Показать"
4. **Скопируйте URL из адресной строки** - это ваша "Старая ссылка"

**Метод 2: По формату**

Стандартный формат URL фильтра Bitrix:
```
/catalog/[IBLOCK_CODE]/filter/[PROPERTY_CODE]-[OPERATOR]-[VALUE]/apply/
```

Примеры:
- `/catalog/asics/filter/crypto-is-btc/apply/` - Только BTC
- `/catalog/asics/filter/manufacturer-is-bitmain/apply/` - Только Bitmain
- `/catalog/asics/filter/crypto-is-btc/manufacturer-is-bitmain/apply/` - BTC + Bitmain

Где:
- `PROPERTY_CODE` - код свойства в нижнем регистре (crypto, manufacturer, algorithm)
- `OPERATOR` - оператор (обычно `is` = равно)
- `VALUE` - значение в нижнем регистре (btc, zec, bitmain)

## 📁 Структура файлов

### Автоматический обработчик (новый):
```
/local/php_interface/init.php
```
- Содержит `AddEventHandler("main", "OnBeforeProlog")`
- Автоматически обрабатывает все SEO фильтры

### Запасной вариант (если нужен):
```
/catalog/asics/filter-zec-crypto/
  ├── index.php     (явная установка фильтра)
  └── .section.php  (название раздела)
```

**Можно удалить**, если автоматический обработчик работает правильно.

## 🧪 Тестирование

**Для проверки работы автоматического обработчика:**

1. Раскомментируйте блок логирования в `init.php` (строки 467-475):
```php
$logFile = $_SERVER['DOCUMENT_ROOT'] . '/.gemini/seo_filter_auto.log';
$logData = date('Y-m-d H:i:s') . ' - SEO Filter Applied' . "\n";
$logData .= 'Request Path: ' . $requestPath . "\n";
$logData .= 'OLD_URL: ' . $oldUrl . "\n";
$logData .= 'SMART_FILTER_PATH: ' . $smartFilterPath . "\n";
$logData .= str_repeat('=', 80) . "\n";
@file_put_contents($logFile, $logData, FILE_APPEND);
```

2. Откройте фильтр в браузере

3. Проверьте лог: `/.gemini/seo_filter_auto.log`

## ⚙️ Настройки

### ID инфоблока модуля SEO фильтра

В `init.php` (строка 426):
```php
$seoIblockId = 14; // ID инфоблока "ЧПУ" (dwstroy_seochpulite)
```

Если ID инфоблока другой, измените это значение.

### Паттерн поиска фильтров

В `init.php` (строка 420):
```php
if (strpos($requestPath, '/filter-') === false) {
    return; // Это не URL фильтра
}
```

Обработчик ищет URL содержащие `/filter-`. Все остальные запросы игнорируются.

## 🎨 Создание фильтров для других каталогов

### Для видеокарт (/catalog/videocard/):

**Старая ссылка:**
```
/catalog/videocard/filter/memory-is-8gb/apply/
```

**Новая ссылка:**
```
/catalog/videocard/filter-8gb-memory/
```

### Для готового бизнеса (/catalog/gotovyy-biznes/):

**Старая ссылка:**
```
/catalog/gotovyy-biznes/filter/crypto-is-ltc-doge/apply/
```

**Новая ссылка:**
```
/catalog/gotovyy-biznes/filter-ltc-doge/
```

## ✅ Преимущества решения

1. **Автоматическая обработка** - не нужно создавать папки для каждого фильтра
2. **Универсальность** - работает для всех каталогов
3. **Простота** - создание нового фильтра = создание записи в инфоблоке
4. **SEO-friendly** - красивые URL + возможность задать мета-теги
5. **Масштабируемость** - легко добавлять новые фильтры

## 🚀 Дальнейшие улучшения (опционально)

1. **Кэширование** - добавить кэширование результатов поиска в инфоблоке
2. **Множественные фильтры** - поддержка комбинированных фильтров (BTC+Bitmain)
3. **Автогенерация** - скрипт для автоматического создания записей для всех значений свойств
4. **Канонические URL** - автоматическая установка `<link rel="canonical">`

---

**Дата создания:** 2025-12-10
**Версия:** 1.0
